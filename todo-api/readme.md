# Flask Todo API with CI/CD

Simple RESTful Todo API built with Flask, including secure ping and health endpoints.
Integrated CI pipeline using GitHub Actions + Bandit (security) + pytest (tests) + flake8 (linting).

## Setup
```bash
pip install -r requirements.txt
python app.py